// Defaults for Minerva Forum Customizer
// Exposed globally so content scripts and the popup can share baseline preferences.
window.MINERVA_DEFAULT_PREFS = {
  theme: 'default',
  fontFamily: 'default',
  headingFont: 'default',
  accentColor: null,
  headerPreset: 'default',
  roundedCards: false
};
